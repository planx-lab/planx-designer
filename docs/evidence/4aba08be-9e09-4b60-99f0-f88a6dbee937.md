# Builtin observability reference: API execution

- Mode: API_NOT_UI. Creation and execution were performed through the real API while the Mac was locked, under explicit user authorization. This is not a UI-created or UI-submitted execution.
- Pipeline: `builtin-observability-reference`, ID `49a00b37-c2a0-4b11-8175-2cfbae9f34ba`, tenant `reference`.
- Execution: `4aba08be-9e09-4b60-99f0-f88a6dbee937`, SUCCEEDED.
- Base: original canonical pipeline `9dd7e28a-6c6f-43b8-8f44-0c10ec35dea2`. Only the metadata name and sink table changed; sink table is `reference.observability_c75fbb72`.
- Actual mutation: one POST `/api/pipelines` using the existing Designer submission contract. It returned an execution ID and started execution; no additional POST `/run` was sent.
- No original pipeline update, Connection metadata write, target clearing, or checkpoint modification was performed.

## Actual detail observations

Source output 8; Mapping, Lookup and Validate output 8; Filter output 7; Sink input/attempted/committed 7/7/7; unknown commit events 0. Source deliveries 3, Source.Ack returned nil 3, ACK errors 0, unknown source acknowledgements 0, pending acknowledgements 0, in-flight batches/bytes 0/0. ACK duration 1334 ns, cleanup duration 72834 ns, cleanup errors 0.

Actual domain snapshots include Mapping CEL cost 52 with 8 samples, Filter CEL cost 24 with 8 samples and 1 filtered record, Validate CEL cost 48 with 16 samples, and Lookup matched 8 with 1 query. These are reported snapshots, not fabricated values; SQL/CSV domain Stats were not included in this backend binary.

Source.Ack returning nil does not independently establish broker-side durable confirmation or exactly-once delivery. Unknown commits count Sink.Write unknown-result events, not rows. Zero pending/in-flight after terminal cleanup is not evidence of successful remote confirmation.

## Artifacts and remaining gates

- `../observability.pipeline.json`: exact lossless configuration supplied through the API.
- `builtin-observability-reference.submission.json`: request and API_NOT_UI attribution.
- `builtin-observability-reference.create-response.json`: actual creation/execution response.
- `4aba08be-9e09-4b60-99f0-f88a6dbee937.detail.json`: actual execution detail.
- API orchestration command exited 0. No retries of mutating requests were performed.
- UI inspection of this existing pipeline/execution, operation probes, compatibility checks, screenshots and YAML export are PENDING. The subsequent UI inspection must not create or rerun the pipeline.
- Actual PostgreSQL reconciliation and main `/api/metrics` collection belong to the parent task and are not claimed here until reported.

## Subsequent real UI inspection (2026-09-09)

After the Mac was unlocked, the existing API-created pipeline and execution were opened in the Designer at `http://127.0.0.1:18081`. No New/Save/Submit/Run was issued for a second pipeline or execution. The UI listed exactly the original and observability executions, both SUCCEEDED.

- SQL Server Source: explicit Validate displayed `Configuration is valid. This does not test the connection.`; the separate real Test connection operation displayed `Connected (read-only probe)`. The connection resource remained version 2. Source compatibility was correctly disabled as unsupported by that component.
- PostgreSQL Sink: real Test connection displayed `Connected (read-only probe)`. A deliberately invalid input schema containing only `amount:string` produced `Static schema incompatible`, `scope:static_schema`, `kind_mismatch` for amount and `missing_column` for the other selected columns. The UI retained the runtime-validation-required warning. This negative precheck is not evidence of runtime data validation.
- Actual Progress displayed source 8, filter 7, sink attempted/committed 7/7, 3 successful Source.Ack returns, and the exact timing/byte/domain snapshots from the saved detail. Broker durability and exactly-once were explicitly not claimed.
- UI exports: `../observability.pipeline.yaml` and `../observability.pipeline.ui.json`. These were exported by the UI from the existing canonical pipeline, not used to imply UI-originated creation or execution.
- Screenshots: `.source-probe.png`, `.incompatible.png`, `.compatibility-issues.png`, `.progress.png`, and `.domain-stats.png` with this execution ID prefix. Corresponding `.source-probe.ui.txt`, `.incompatible.ui.txt`, and `.progress.ui.txt` capture the actual DOM evidence.
- Positive static compatibility with the precise post-Mapping/Lookup schema remains PENDING the domain owner's schema confirmation. No temporal precision or nullability was guessed from legacy Discover.
- Source typed data preview is a separately agreed API/UI implementation task, not verified by the pipeline Spec Preview export above.

## Parent runtime metrics and target reconciliation

The parent captured the real main-process metrics at `/api/metrics` in [runtime-main-metrics-20260909.prom](/Users/mw/workspace/repo/github.com/planx-lab/planx-engine/docs/runtime-main-metrics-20260909.prom). Reported observations: source records 8 / batches 3, sink records 7 / batches 3, ACK-confirmed counter 3 (Source.Ack returned nil), pending/age/in-flight/retained/active 0, and one successful cleanup for each of six nodes. Byte labels explicitly identify typed estimates; the parent reported no tenant/pipeline/node/resource high-cardinality labels. These are parent-collected metrics, not independent proof of broker durability. The running engine-operations SHA256 was e5d8ee2985f9bc099604a36895a0419946ef274df9998d0b15e6c038bf481430.

The parent also completed a read-only bidirectional EXCEPT for the new target with exit 0: source 8, filtered 1, expected 7, actual 7, missing 0, unexpected 0. All ten columns were compared. Expected rows came from the PostgreSQL source/dimension mirror previously reconciled with the initial SQL Server fixture; this does not claim a second SQL Server extraction. No pipeline was rerun for this reconciliation.

## Designer Preview readiness and identity regression

Preview client/UI implementation is ready for the separately deployed backend hook. It preserves public typed schema/cells as lossless JSON, uses explicit 20-row/65536-byte defaults with hard caps, distinguishes absent/unsupported capabilities, aborts stale or cancelled requests, and fails closed on sensitive-schema/limit/timeout errors. This is frontend test/build evidence, not real-source Preview verification.

The initial implementation passed 212 tests but emitted an actual React duplicate-key warning because operation and preview panels shared a sibling key. The user explicitly approved the one-line identity fix. A focused regression first failed with that exact warning (exit 1); only the Preview key was prefixed with `preview:`. No pipeline data changed.

Commands from the Designer directory after the fix:

- `npm test -- --no-cache --reporter=dot`: exit 0, 213/213 tests, 28/28 files, no duplicate-key warning.
- `npm test -- --no-cache --configLoader runner --reporter=dot`: exit 0, 213/213 tests, 28/28 files.
- `npm run build`: exit 0; existing bundle-size warning only.
- `make -C ../planx-engine ui`: exit 0; generated UI staged through the existing Makefile target.

The focused RED command was `npm test -- --no-cache src/components/editor/ConfigPanel.preview.test.tsx -t 'keeps source preview identity separate' --reporter=dot`; only that test was selected. No tests were deleted or marked skipped, and the subsequent full suites ran all 213 tests.

## Actual typed Source Preview and conservative static compatibility (2026-09-09)

The parent confirmed `engine-preview` ready on port 18080 with the same SQLite and advertised SQL Server Source preview capability. CUA used the existing Designer dev UI on port 18081, refreshed the stale catalog, and reopened `builtin-observability-reference`. No pipeline Save, Submit, Run, Connection metadata POST, checkpoint change, or target write was requested. Both existing executions remained SUCCEEDED; no active execution was owned by this task.

The actual UI **Preview source data** action used the existing `sqlserver/source` table configuration (`reference.source_records`, Connection `source` version 2), with the displayed defaults of 20 rows and 65536 response bytes. It returned **8 preview records**, **Not truncated (reported by source)**. The rendered typed schema and records preserved signed int64 extrema, values above 2^53, 38-digit decimal coefficients with exponent -18, explicit NULL, empty strings, UTF-8, and UTC timestamp text. These are source-preview results, not another pipeline execution or ACK proof.

Artifacts were extracted from the actual UI's raw schema/typed-record displays without numeric or temporal reformatting:

- `4aba08be-9e09-4b60-99f0-f88a6dbee937.source-preview.schema.json`
- `4aba08be-9e09-4b60-99f0-f88a6dbee937.source-preview.records.json`
- `4aba08be-9e09-4b60-99f0-f88a6dbee937.source-preview.ui.txt`
- `4aba08be-9e09-4b60-99f0-f88a6dbee937.source-preview.png`

The complete ten-column Sink precheck input retained this actual Source schema, applied the existing Mapping's declared amount Decimal(38,18)/nullable contract, and appended the Lookup owner's confirmed non-null string `dimension_label` and int64 `dimension_version` fields. Neither time precision nor string length was invented. The existing PostgreSQL sink remained `reference.observability_c75fbb72`.

The actual UI **Check compatibility** result was **Static schema incompatible**, scope `static_schema`, runtime validation still required, with exactly these displayed issues:

| Field | Code |
| --- | --- |
| tenant_key | unknown_precision |
| occurred_at | unknown_precision |

SQL owner Meitner confirmed both are conservative metadata outcomes: the old public timestamp field has no fractional-time precision, and public String has no length upper bound to prove all values fit a bounded varchar target. Decimal precision/scale are not a substitute. New `timePrecision` can address the temporal item only when the actual source precision fits the target; it does not resolve the String length item. Successful prior aligned-row execution and PG reconciliation do not establish static compatibility for the entire value domain. **No positive compatibility claim is made.**

- `4aba08be-9e09-4b60-99f0-f88a6dbee937.compatibility-old-metadata.input-schema.json`
- `4aba08be-9e09-4b60-99f0-f88a6dbee937.compatibility-old-metadata.ui.txt`
- `4aba08be-9e09-4b60-99f0-f88a6dbee937.compatibility-old-metadata.png`

## Optional temporal metadata UI contract and regression evidence

Following the accepted ADR-017 refinement and SDK owner's exact contract, `RecordField` now accepts optional lowerCamel `timePrecision`. Only DateTime/Timestamp permit 0..9 in the domain contract; omitted means unknown, and explicit 0 means whole seconds. Nested Fields/Element use the same type. The existing generic lossless decoder and JSON/YAML serializers already preserve these properties and timestamp strings, so no new parser, inference, value rounding, or Decimal-slot reuse was added. Preview now explains the distinction in its existing style. SDK validation remains the schema/value authority.

Only `src/types/record.ts`, `src/components/editor/SourcePreviewPanel.tsx`, and the new `src/components/editor/SourcePreviewPanel.temporal.test.tsx` were changed for this temporal increment. Regressions cover actual-property retention through Preview -> compatibility, missing versus 0/6/9 including nested schemas, unmodified nanosecond timestamp/civil text, and canonical JSON/YAML with exact decimal and >2^53 config literals.

Commands executed from `planx-designer`:

| Command | Exit | Evidence |
| --- | --- | --- |
| `npm test -- --no-cache src/components/editor/SourcePreviewPanel.temporal.test.tsx --reporter=dot` | 1 | RED: 1 failed / 3 passed; missing explanatory UI text, existing lossless transport/roundtrip already passed. `/tmp/planx-designer-temporal-red.log` |
| `npm test -- --no-cache --reporter=dot` | 0 | GREEN: 217/217 tests, 29/29 files. `/tmp/planx-designer-temporal-green.log` |
| `npm test -- --no-cache --configLoader runner --reporter=dot` | 0 | GREEN: 217/217 tests, 29/29 files. `/tmp/planx-designer-temporal-runner.log` |
| `npm run build` | 0 | TypeScript and production build passed. `/tmp/planx-designer-temporal-build.log` |
| `make -C ../planx-engine ui` | 0 | Existing build/staging mechanism only. `/tmp/planx-designer-temporal-stage.log` |

No tests were deleted or skipped. No install or dependency graph change occurred. The only build warning was the existing >500 kB chunk warning. Generated main asset is `index-DxUVBYlb.js`; this staging does not hot-replace the currently running backend binary. The earlier Preview sibling-key RED/GREEN remains included in the full suite.

Real UI verification against a backend containing the new SDK/SQL `timePrecision` metadata remains pending parent deployment confirmation. Original execution `dfadaa6e-f42a-40ba-aaad-166a75ff62ea` remains UI-created/UI-run; observability execution `4aba08be-9e09-4b60-99f0-f88a6dbee937` remains **API_NOT_UI** creation/run, with subsequent real UI inspection, probes, static prechecks and Source Preview recorded separately.

## Live temporal-help UI with metadata deployment still pending

At 2026-09-08T18:50:25.662Z, CUA reopened the existing observability pipeline and invoked only bounded Source Preview. It returned 8 records again; the actual `occurred_at` schema still omitted `timePrecision`. The new 217-test UI visibly explained omitted = unknown and 0 = whole seconds, without inferring precision from timestamp text or Decimal slots. Screenshots/DOM: `4aba08be-9e09-4b60-99f0-f88a6dbee937.source-preview-temporal-help.png` and `.source-preview-temporal-help.ui.txt`. No repeat compatibility request was sent for the unchanged old metadata. The p6-removes-only-temporal-diagnostic gate remains pending the parent-confirmed metadata deployment. The independent `tenant_key` length unknown must remain, and no all-true claim, maxLength extension, schema/target/data change, Save, Submit, Run, or resource POST is authorized by this diagnostic gate. Preview completed; no active operation or execution is owned by this task.

## Final backend UI gate: endpoint unavailable (2026-09-08T19:44:22Z)

The parent reported replacing the old engine-preview with `/tmp/planx-builtin-ui/engine-final` on port 18080, same SQLite/resources/executions, session 79384, SHA256 `0fc6ac1e2a42ff86b48976d2c193ba1de02c466edd63d170623c9f470718d1e5`. This deployment identity is parent-reported, not independently hashed by the UI task.

CUA rebound the original Chrome tab `2078542068` at `http://127.0.0.1:18081/`, selected tenant `reference`, and opened Pipelines. The actual UI displayed `Engine status: Offline` / `Engine unreachable` and `Failed to load pipelines.` One explicit read-only Retry completed with the same error. The browser error-log accessor returned no additional entries; it did not establish the underlying network cause.

**Final p6 diagnostic gate is BLOCKED / NOT VERIFIED at this observation**, not passed: the existing pipeline could not be opened, so no Source Preview or compatibility request was made. No value of timePrecision was inferred, and no disappearance of occurred_at or change to tenant_key was claimed. The parent was notified to check the upgraded service's reachability. The existing tab is preserved for continuation after confirmation.

No source, configuration, proxy, resource metadata, target data, or checkpoint was changed. No pipeline was saved, created, submitted, or run. No tests, build, or staging were repeated; the previously recorded 217-test evidence remains unchanged. This continuation only appends this owned evidence document.

## Final single-Engine read-only UI gate: VERIFIED diagnostic transition

Observed at 2026-09-08T19:49:27.907Z in the original Chrome tab `2078542068`, now using the Engine-owned same-origin UI at `http://127.0.0.1:18080/`. The parent identified this deployment as `/tmp/planx-builtin-ui/engine-final`, session 79384, SHA256 `0fc6ac1e2a42ff86b48976d2c193ba1de02c466edd63d170623c9f470718d1e5`, with embedded 217-test asset `index-DxUVBYlb.js`. Binary identity/asset hash attribution is parent-reported; this UI task did not rebuild or rehash it.

### Preserved 18081 failure boundary

The prior browser observations of `Engine Offline` and `Failed to load pipelines` at port 18081 remain valid evidence. Reading the existing browser error/warn accessor returned no entries, so no browser-observed HTTP status or CORS diagnosis was fabricated. The parent subsequently isolated the cause: direct 18080 `/api/healthz` and `/api/pipelines?tenantId=reference` returned HTTP 200, while 18081 `/api/healthz` failed immediately with curl exit 7 / connection refused because the development server was no longer listening. This was not an Engine failure or a production proxy repair. At the parent's explicit direction, the same tab navigated to 18080; no development server, proxy, source, or configuration was changed.

### Actual final UI actions and results

The embedded UI displayed Ready. Its reference Pipelines page showed the two original pipelines, each SUCCEEDED with exactly one execution. CUA opened only the existing `builtin-observability-reference` (pipeline `49a00b37-c2a0-4b11-8175-2cfbae9f34ba`). Source remained `sqlserver/source`, table `reference.source_records`, Connection `source` version 2, batch_rows 3. Sink remained `postgres/sink`, table `reference.observability_c75fbb72`, Connection `target` version 2 and the existing ten-column/upsert configuration.

One explicit **Preview source data** action used displayed limits 20 rows / 65536 response bytes and returned **8 preview records**, **Not truncated (reported by source)**. The actual returned `occurred_at` field had `kind: timestamp`, `nullable: true`, and **timePrecision: 6**. Decimal precision/scale were left as returned; no precision was inferred from values or placed in Decimal slots.

Actual Source schema copied from the UI display:

```json
{
  "fields": [
    {
      "name": "record_id",
      "kind": "int64",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "tenant_key",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "dimension_key",
      "kind": "int64",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "amount",
      "kind": "decimal",
      "nullable": true,
      "precision": 38,
      "scale": 18
    },
    {
      "name": "occurred_at",
      "kind": "timestamp",
      "nullable": true,
      "precision": 0,
      "scale": 0,
      "timePrecision": 6
    },
    {
      "name": "nullable_text",
      "kind": "string",
      "nullable": true,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "empty_text",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "unicode_text",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    }
  ]
}
```

One **Check compatibility** action used the real Preview schema after the already-configured amount Mapping declaration and the Lookup owner's confirmed two appended fields. The source temporal field, including timePrecision 6, was retained unchanged. This JSON was entered only into the transient **Input record.Schema JSON** precheck textbox, not saved into pipeline config. No maxLength, wider target, fixture alteration, or fabricated metadata was introduced. Full precheck input:

```json
{
  "fields": [
    {
      "name": "record_id",
      "kind": "int64",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "tenant_key",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "dimension_key",
      "kind": "int64",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "amount",
      "kind": "decimal",
      "nullable": true,
      "precision": 38,
      "scale": 18
    },
    {
      "name": "occurred_at",
      "kind": "timestamp",
      "nullable": true,
      "precision": 0,
      "scale": 0,
      "timePrecision": 6
    },
    {
      "name": "nullable_text",
      "kind": "string",
      "nullable": true,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "empty_text",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "unicode_text",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "dimension_label",
      "kind": "string",
      "nullable": false,
      "precision": 0,
      "scale": 0
    },
    {
      "name": "dimension_version",
      "kind": "int64",
      "nullable": false,
      "precision": 0,
      "scale": 0
    }
  ]
}
```

Actual displayed result:

```text
Static schema incompatible.
Scope: static_schema
Runtime validation is still required; values and execution have not been verified.
Compatibility issues:
Field        Code
tenant_key   unknown_precision
```

The table contained only its header and this one issue row (actual extracted rows: `["FieldCode","tenant_keyunknown_precision"]`). **occurred_at no longer had unknown_precision or precision_loss**, while **tenant_key unknown_precision remained**. This verifies the requested real p6 Source -> existing PG p6 diagnostic transition. It does not claim whole-schema `compatible:true`: the independent bounded-String length proof remains unavailable, and runtime value validation is still required. The inline CUA screenshot showed this one-row diagnostic and the static/runtime disclaimer. No additional screenshot or JSON files were written in this final slice, per the evidence-document-only scope.

### Frozen handoff

Both read-only operations completed. No pipeline was saved, created, submitted, or executed; no resource version, target data, checkpoint, schema, or original execution object was changed. Original execution `dfadaa6e-f42a-40ba-aaad-166a75ff62ea` retains its original UI-created/UI-run provenance. Observability execution `4aba08be-9e09-4b60-99f0-f88a6dbee937` remains API_NOT_UI creation/run, with this later **real embedded-UI Preview/precheck** recorded separately.

No 217-test rerun, install, build, staging, source edit, or backend edit occurred. The only file changed by this final continuation is this owned evidence document. The original browser tab is retained at the final diagnostic view; no user window was closed. This slice is frozen and returned to the parent.
