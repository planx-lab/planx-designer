# eslint-plugin-jsx-a11y@6.x declares peer eslint ^6||^7||^8 but works on
# eslint 9/10 (stale peer range, not a real incompatibility). Use npm's
# legacy peer-dep resolution so installs succeed on clean environments (CI).
legacy-peer-deps=true
